---
import BaseLayout from '../../layouts/BaseLayout.astro'; import { engineeredHues } from '../../lib/catalogue'; import EngineeredBrowser from '../../components/engineered/EngineeredBrowser.tsx'; const base=import.meta.env.BASE_URL.replace(/\/$/,'');
---
<BaseLayout title="Engineered hues — Chroma"><div class="eyebrow">Engineered hues</div><h1>Colours for<br/>immediate impact.</h1><EngineeredBrowser client:load hues={engineeredHues} base={base}/></BaseLayout>
